// Handlebars HTML render. PRD §9.
// TODO Day 4: compile src/templates/resume.hbs, pass resume_json_rewritten,
// return HTML string. Skipped sections must collapse entirely (no empty headers).

async function renderHtml(_resumeJson) {
  throw new Error('renderHtml not implemented (Day 4)');
}

module.exports = { renderHtml };
