node_modules/

# Secrets — block all .env files, allow only the placeholder example
.env
.env.*
!.env.example

# Credentials / keys (just in case anyone drops one in by mistake)
*.pem
*.key
*.p12
*.pfx
secrets/
credentials.json
service-account.json

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Build / runtime
dist/
coverage/
.cache/
tmp/
.runtime/

# Puppeteer cached browsers (downloaded into ~/.cache, but local override possible)
.puppeteer-cache/

# Real resume samples used locally to design role archetypes / templates.
# These contain PII (names, phone, email) — NEVER commit them to the public repo.
docs/resume-samples/
